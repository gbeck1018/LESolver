#pragma once

#include <climits>

#include <string>
using std::string;
#include <unordered_map>
using std::unordered_map;

#define UNINITIALIZED_ENTROPY INT_MAX
enum class GameType {Wordle, Nerdle, UnknowType};
